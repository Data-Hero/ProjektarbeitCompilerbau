string concat(string a, string b) {
    string c;
    c = a + b;
    return c;
}

int plusOne(int x) {
    int y = x + 1;
    return y;
}

if (true) {
    string path = "./src/main/test/input.txt";
    string fileContent = read(path);
    write(fileContent, "./src/main/test/out.txt");
}

int x = 5;
x = x + 5;

double y = 1.2;
string z = "Hello " + "world!";


// Array test
string[][] arr1;
//string[] arr2 = "arr2";
int[] arr2 ;
arr1 = arr1 + arr2;

int b = plusOne(1);
int c;
c = 6;

//int x = test@{
//    :punct: { return x; }
//};

// 1a comment test


string test = "test.txt";

read("abc.txt");

string input = read(test);

c = 1;
while (c < 5) {
    if (c == 2) {
        test = test + c;
        write("c == 2");
    } else if (c == 3) {
        test = test + c;
        write("c == 3");
    } else {
        test = test + c;
        write("test = test + c;");
    }

    write("while");

    c = c + 1;
}

string[] arr;
foreach (string i in arr) {
  int t1 = length("zwei");
  double t2 = convert(double, 5);
  bool t3 = isType(bool, y);
}